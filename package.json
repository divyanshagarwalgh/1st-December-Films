{
  "name": "fdf-script-analyser",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && astro preview",
    "test": "vitest run",
    "db:local": "wrangler d1 migrations apply DB --local",
    "cf:types": "wrangler types"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "@astrojs/cloudflare": "^14.3.0",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.8.3",
    "vitest": "^5.0.0",
    "wrangler": "^4.129.0"
  }
}
