---
// The page lives in the Webflow Designer at /script; it loads this app's embed.js and embed.css
// and posts to /api/analyse. The app root only sends people to the page. Relative so the browser
// stays on whichever host it came from (1stdecember.com or the webflow.io staging domain).
import { redirectTo } from "../lib/request";
return redirectTo("/script", 302);
---
